ortools>=9.10.0
pandas>=2.0.0
numpy>=2.0.0
pytest>=8.0.0
# Operations API (api/): sign-in, saved actions, alerts, evidence photos.
fastapi>=0.115
uvicorn>=0.30
# Test client for tests/test_api.py
httpx>=0.27
joblib>=1.3.0
scikit-learn>=1.3.0
# Required to load the Neev failure-risk classifier (neev_failure_risk_model.cbm)
# in NEEV_FINAL_MODULE/neev_model_demo.ipynb. Not needed by the optimizer, which
# consumes the exported predictions CSV.
catboost>=1.2
